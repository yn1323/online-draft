import { AvatarSelectionModal } from './index';

describe('AvatarSelectionModal', () => {
  test('コンポーネントが正常に定義されている', () => {
    expect(AvatarSelectionModal).toBeDefined();
    expect(typeof AvatarSelectionModal).toBe('function');
  });
});
