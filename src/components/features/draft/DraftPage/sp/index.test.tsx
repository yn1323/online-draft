import { DraftPageSp } from './index';

describe('DraftPageSp', () => {
  test('コンポーネントが正常に定義されている', () => {
    expect(DraftPageSp).toBeDefined();
    expect(typeof DraftPageSp).toBe('function');
  });
});
